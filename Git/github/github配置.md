```shell
##用户名标识  ---- 实际也可以填写您的github仓库的名称
##邮箱标识  -------可以填写github仓库的邮箱 

git config --global user.name "rerere"  
git config --global user.email "xxxx@xxx.com"  

git config --global user.name "Rerere"  
git config --global user.email "550074710@qq.com"  

##创建秘钥
ssh-keygen -t rsa  
```

