# 注册中心

1、什么是服务发现

定义：在分布式架构中，找到机器的物理地址



两大作用：

​	快速地对在环境中运行的服务器数量进行水平伸缩（随时添加更多服务器）

​	提高引用的弹性，绕过不可用服务（自动移除不可用服务）



特点

- 高可用
- 点对点
- 负载均衡
- 有弹性
- 容错







服务发现架构

- 服务注册
- 服务地址的客户端查找
- 信息共享
- 健康监测



### 服务注册

服务实例：

服务ID：标识一组相同服务的键

IP地址：

端口

### 服务地址的客户端查找



### 信息共享



### 健康监测





### 客户端负载均衡原理

当服务消费者需要调用一个服务时：

它将联系服务发现服务，获取它请求的所有服务实例，然后在服务消费者机器上本地缓存数据

每当客户端，需要调用该服务时，服务消费者将从缓存中查找该服务的位置信息，

然后，客户端将定期与服务发现服务联系，并刷新服务实例的缓存，客户端缓存最终是一致的，但是始终存在这样的风险：在客户端联系服务发现实例以进行刷新和调用时，调用可能会被指定到不健康的服务实例上。  





常见的服务发现组件之间的区别？

Eureka、Zookeeper、Consul，Nacos、Etcd



问题：

DNS与服务发现的区别

dns 做了两件事，路由，负载均衡

为什么不用DNS或者负载均衡器实现？





