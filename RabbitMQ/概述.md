# 概述

消息通信

异步

分隔了数据的发送，和接受来解耦应用

在任何相同的模式下以相同的方式工作



路由器，物流公司，投递服务，DNS，缓存



消息：

载荷（payload）：要传输的数据，可以是任何内容，json、二进制

标签（label）：描述有效载荷，并决定谁将获得消息的拷贝



发后既忘：单向方式



过程：

生产者创建消息，消费者接受这些消息



信道（Channel）：tcp上的虚拟连接



队列